[project]
name = "shopstream-generator"
version = "0.0.0"
description = "Seeded, deterministic, deliberately messy e-commerce data generator (built in Week 3)."
requires-python = "==3.13.*"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.18,<0.13"]
build-backend = "uv_build"
